# MiClassics

This branch places the landing page in `web/` to reduce merge conflicts with repositories that already have root-level frontend files.

## Run locally

Open `web/index.html` directly in a browser, or serve the folder with:

```bash
cd web
python3 -m http.server 8080
```
